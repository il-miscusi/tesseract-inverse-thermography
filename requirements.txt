# Host-side requirements, pinned to the exact versions every number in the
# README and write-up was produced with. Each Tesseract carries its own,
# isolated, in its container.
tesseract-core[runtime]==1.11.0
tesseract-jax==0.4.1
numpy==2.5.1
scipy==1.18.0
matplotlib==3.11.1
jax==0.11.1
jaxlib==0.11.1
torch==2.13.0
pytest==9.1.1
